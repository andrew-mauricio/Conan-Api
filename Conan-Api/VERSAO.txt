Conan-Api 2.10.0 — plugin API for Conan Exiles Enhanced
API table: v8   (a plugin asks for at most this, via MinApiVersion)
packaged at: 28/08/2026 08:47:52 -03
target game: Unreal Engine 5.6.1 (++exiles+release) build 24922388

WARNING: this API's offsets belong to that game build. When Conan
updates, no plugin loads — on purpose — and the reason appears in
Conan-Api/Logs/ConanLoader.log. That is not a defect: it is the API
refusing to read memory that moved.

──────────────────────────────────────────────────────────────────
REBUILT IN THIS PACKAGE — this is what you have in your hands
──────────────────────────────────────────────────────────────────
Built from scratch NOW. The sha256 below changes on every package
(build timestamps go into the binary) even with not ONE line of code
changed. These are not, byte for byte, the ones running on the server
when the proofs in Docs/ESTADO.md were taken: they are a NEW build of
the SAME source. What guarantees behaviour is the source and the
gates, not byte equality.

  c71ed63f9edcc14b  Conan-Api/Plugins/ConanShop/ConanShop.dll
  ba87661b97b9602e  Conan-Api/Plugins/Permission/ConanPermission.dll
  208b8e553c0bd5a2  winmm.dll

──────────────────────────────────────────────────────────────────
VALIDATED RUNNING ON A SERVER — the bytes behind the proofs
──────────────────────────────────────────────────────────────────
deployed at: 2026-08-27 00:24:25
(the sha here DIFFERS from the one above on purpose: different
 build, same source — which is what shows the byte difference is
 only the build.)
  84db645f3f39e5ca  winmm.dll
